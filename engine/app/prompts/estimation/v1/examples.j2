### Example A — internal tool (line_items style)
**Project:** Shift handover log for a factory floor tablet app.  
**Estimate:** Auth (LDAP) 40h; CRUD shifts 60h; offline sync 80h; QA 32h — total ~212h.

### Example B — mobile_app (tabular phases)
**Project:** Field inventory scanner with barcode and photo upload.  
**Phases:** Discovery 24h | Build 120h | Hardening 40h | Release 16h — note confidence per row in full output.

### Example C — data_pipeline (narrative style)
**Project:** Nightly sales CSV to warehouse with anomaly alerts.  
**Narrative:** Ingestion service, idempotent loads, Slack alerts on schema drift; rough order of weeks and team roles only.
